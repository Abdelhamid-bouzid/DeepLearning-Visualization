In this notebook we summarize the different steps of retrieving the images that mostly stimulate a given node in the model.
we also show the what kind of features the given node has learned from these images.
